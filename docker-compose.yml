services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gasty
      POSTGRES_PASSWORD: gasty
      POSTGRES_DB: gasty
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
